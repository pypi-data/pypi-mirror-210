syntax = "proto3";
package auth_service.proto;

service AuthService {
  rpc IsUserAuthenticated(IsUserAuthenticatedRequest)
      returns (IsUserAuthenticatedResponse) {}
}

// Requests
message IsUserAuthenticatedRequest { string access = 1; }

// Responses
message IsUserAuthenticatedResponse { uint64 user_id = 1; }
