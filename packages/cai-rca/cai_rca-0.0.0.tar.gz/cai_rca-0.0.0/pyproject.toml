[tool.poetry]
name = "cai-rca"
version = "0.0.0"
description = "Causal AI: Root Cause Analysis (RCA)"
authors = ["causaLens <opensource@causalens.com>"]
readme = "README.md"
packages = [{include = "cai_rca"}]

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
