[tool.poetry]
name = "hdcc"
version = "0.6.4"
description = ""
authors = ["Yifan Yang <yyang29@stanford.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
numpy = ">=1.19.2"

[tool.poetry.dev-dependencies]
pytest = "^7.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
