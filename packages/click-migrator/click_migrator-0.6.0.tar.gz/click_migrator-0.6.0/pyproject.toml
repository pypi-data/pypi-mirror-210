[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "click_migrator"
version = "0.6.0"
description = "Solution for executing ClickHouse database migration scripts"
readme = "README.md"
requires-python = ">=3.8"
license = {file = "LICENSE"}
authors = [
    {name = "Evgrafov Alexander", email = "evgrafov.alexander92@yandex.ru"}
]
dependencies = [
    "clickhouse-driver"
]

[project.urls]
Repository = "https://github.com/EvgrafovAlexander/clickhouse_migrator"
