[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "pingsafe_cli"
version = "0.0.1"
authors = ["pingsafe-dev <dev@pingsafe.com>"]
description = "PingSafe CLI is an extension of our vision to shift-left security with PingSafe. It is the newest edition to the toolset that will help to keep our customer’s cloud infrastructure safe from external threats and ensure the highest quality standard for security while writing and deploying code."
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7.1"
importlib_metadata = "6.6.0"
jsonschema = "4.17.3"
requests = "2.29.0"
toml = "0.10.2"

[tool.poetry.scripts]
pingsafe-cli = "pingsafe_cli.scanner.cli.main:start"