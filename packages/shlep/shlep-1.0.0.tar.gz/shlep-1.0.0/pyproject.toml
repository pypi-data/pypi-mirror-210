[project]
name = "shlep"
version = "1.0.0"
description = "gather directory contents into a single file"
authors = [
    { name = "Stephan Fitzpatrick", email = "knowsuchagency@gmail.com" },
]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "Apache-2.0"

[project.scripts]
shlep = "shlep.main:cli"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
