poetry-lock-without-upgrade-deps:
	poetry lock --no-update
install:
	poetry install
build:
	poetry build
env-install:
	conda create -y --name faceid_lib python==3.8.0
env-activate:
	conda activate faceid_lib