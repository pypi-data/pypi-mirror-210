[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "refextract_cli"
description = "Command line interface for refextract"
readme = "README.md"
version = "0.1.2"

